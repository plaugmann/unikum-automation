# Direkte afhaengigheder. Bevidst uden noejagtige laasninger: en laasefil fra
# "pip freeze" paa Windows braekker paa ARM64, hvor nogle pakker ikke har hjul
# i netop den version - og pip afbryder saa hele installationen.

httpx>=0.28
python-dotenv>=1.0

# Browser til at forny Unikum-sessionen. Paa ARM64 bruges systemets chromium
# via CHROMIUM_PATH; koer ikke "playwright install" der.
playwright>=1.50

# Tekst ud af beskeder og bilag
beautifulsoup4>=4.13
lxml>=5.0
pypdf>=5.0
python-docx>=1.1

# Opsummering
openai>=2.0

# Lokal webserver. Bevidst uden [standard]-ekstraerne: uvloop og httptools
# skal oversaettes paa ARM og giver ingen gevinst ved vores belastning.
fastapi>=0.115
uvicorn>=0.30
